import { XL, M, Hint, A, B } from "@ui";

export const INIT_MDX = `
  <M>
     Prelude content
  </M>`;

export const COMPONENTS = {
  XL,
  M,
  Hint,
  A,
  B,
};

export const TAGS = Object.keys(COMPONENTS);
